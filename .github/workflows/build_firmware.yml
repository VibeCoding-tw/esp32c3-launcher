name: ESP32-C3 Firmware Build

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch: # 允許手動觸發

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write # 必要的權限來建立 Release

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Cache PlatformIO
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/pip
            ~/.platformio/.cache
          key: ${{ runner.os }}-pio

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install PlatformIO
        run: |
          python -m pip install --upgrade pip
          pip install --upgrade platformio

      - name: Build Firmware
        run: pio run

      - name: Extract Version
        run: |
          set -euo pipefail
          VERSION=$(sed -n 's/.*CURRENT_VERSION = "\([^"]*\)".*/\1/p' src/main.cpp | head -n 1)
          test -n "$VERSION"
          printf '%s\n' "$VERSION" > .pio/build/esp32c3-launcher/version.txt
          echo "Building Version: $VERSION"

      - name: Verify Release Assets
        run: |
          set -euo pipefail
          ls -lh .pio/build/esp32c3-launcher/firmware.bin .pio/build/esp32c3-launcher/version.txt
          test -s .pio/build/esp32c3-launcher/firmware.bin
          test -s .pio/build/esp32c3-launcher/version.txt

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: esp32c3-firmware
          path: |
            .pio/build/esp32c3-launcher/firmware.bin
            .pio/build/esp32c3-launcher/version.txt

      - name: Create Release
        if: github.ref == 'refs/heads/main' # 僅在 main 分支 push 時執行
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest
          name: "Latest Build"
          body: "Auto-generated firmware and version info from GitHub Actions"
          files: |
            .pio/build/esp32c3-launcher/firmware.bin
            .pio/build/esp32c3-launcher/version.txt
          make_latest: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
